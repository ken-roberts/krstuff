#!/bin/bash
# Lists plugins actually loaded in the .grails directory for this project.
# This looks at the physical .grails directory for this project, not anything in the source directories.

APP=`pwd | sed 's/..*\///'`
#echo $APP

PLUG="$HOME/.grails/1.3.4/projects/$APP/plugins/"
ls -l $PLUG
