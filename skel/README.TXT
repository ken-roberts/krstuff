This directory is for initial installations.

Each subdirectory corresponds to a 'uname -s' command output for the appropriate platform.

Then edit things, especially /etc/krstuff/homes, to fit your local setup.
